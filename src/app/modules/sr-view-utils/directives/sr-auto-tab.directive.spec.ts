import { SrAutoTabDirective } from './sr-auto-tab.directive';

describe('SrAutoTabDirective', () => {
  it('should create an instance', () => {
    const directive = new SrAutoTabDirective();
    expect(directive).toBeTruthy();
  });
});
