import { SrDateMaskDirective } from './sr-date-mask.directive';

describe('SrDateMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new SrDateMaskDirective();
    expect(directive).toBeTruthy();
  });
});
